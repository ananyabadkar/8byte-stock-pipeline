# Copy this file to .env and fill in real values. .env is gitignored --
# never commit real credentials.

# --- Alpha Vantage ---
# Get a free key at https://www.alphavantage.co/support/#api-key
ALPHA_VANTAGE_API_KEY=4N0OC6OFG6O1DSC8

# Comma-separated list of ticker symbols to track
STOCK_SYMBOLS=IBM,AAPL,MSFT

# --- PostgreSQL (shared instance; separate databases for Airflow vs stock data) ---
POSTGRES_USER=airflow
POSTGRES_PASSWORD=change_this_password
STOCK_DB_NAME=stockdata

# --- Airflow webserver admin login ---
AIRFLOW_ADMIN_USER=admin
AIRFLOW_ADMIN_PASSWORD=admin123
